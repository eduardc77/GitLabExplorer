query GetProjects($first: Int = 20, $after: String, $sort: String) {
  projects(
    first: $first
    after: $after
    visibilityLevel: public
    sort: $sort
  ) {
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
    edges {
      cursor
      node {
        id
        name
        path
        fullPath
        description
        visibility
        avatarUrl
        webUrl
        starCount
        forksCount
        lastActivityAt
        createdAt
        updatedAt
        repository {
          rootRef
          empty
        }
        namespace {
          id
          name
          path
          fullPath
        }
      }
    }
  }
} 